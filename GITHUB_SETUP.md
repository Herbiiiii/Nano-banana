# 🚀 Инструкция по публикации на GitHub

## Шаги для публикации проекта

### 1. Инициализация Git репозитория

```bash
git init
```

### 2. Добавление всех файлов

```bash
git add .
```

### 3. Первый коммит

```bash
git commit -m "Initial commit: Nano Banana Pro Web UI"
```

### 4. Создание репозитория на GitHub

1. Перейдите на [github.com](https://github.com)
2. Нажмите кнопку **"New"** или **"+"** → **"New repository"**
3. Введите название репозитория (например: `nano-banana-pro-ui`)
4. Выберите **Public** или **Private**
5. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)
6. Нажмите **"Create repository"**

### 5. Подключение к удаленному репозиторию

GitHub покажет вам команды. Выполните:

```bash
git remote add origin https://github.com/ВАШ_USERNAME/nano-banana-pro-ui.git
git branch -M main
git push -u origin main
```

Замените `ВАШ_USERNAME` на ваш GitHub username.

### 6. Готово! 🎉

Ваш проект теперь на GitHub!

## Дополнительные настройки (опционально)

### Добавить описание репозитория

На странице репозитория нажмите ⚙️ рядом с "About" и добавьте:
- **Описание**: "Веб-интерфейс для Google Nano Banana Pro через Replicate API"
- **Темы**: `streamlit`, `ai`, `image-generation`, `replicate`, `nano-banana-pro`

### Добавить скриншоты

Создайте папку `screenshots/` и добавьте скриншоты интерфейса, затем обновите README.

## Важно! ⚠️

- **НЕ** коммитьте файлы с API ключами
- `.gitignore` уже настроен для защиты конфиденциальных данных
- Проверьте, что файл `.env` не попал в репозиторий

## Проверка перед публикацией

```bash
# Проверьте, что нет конфиденциальных данных
git status
git diff

# Убедитесь, что .gitignore работает
git check-ignore -v .env
```

