# 🐳 Docker версия с MinIO и PostgreSQL

Эта версия приложения использует:
- **MinIO** для хранения изображений
- **PostgreSQL** для хранения истории генераций

## 🚀 Быстрый запуск

```bash
# 1. Создайте .env файл
cp .env.example .env
# Отредактируйте .env и добавьте REPLICATE_API_TOKEN

# 2. Запустите все сервисы
docker-compose up -d

# 3. Откройте браузер
# http://localhost:8501
```

## 📦 Что включено

- **PostgreSQL** - база данных (порт 5432)
- **MinIO** - хранилище изображений (порты 9000, 9001)
- **Streamlit App** - веб-интерфейс (порт 8501)

## 💾 Хранение данных

### Изображения → MinIO
- Все сгенерированные изображения сохраняются в MinIO
- Доступ через веб-интерфейс: http://localhost:9001
- Логин: `minioadmin` / `minioadmin123`

### История → PostgreSQL
- Промпты, параметры, метаданные
- Доступ через любой PostgreSQL клиент
- Данные сохраняются между перезапусками

## 📖 Подробная инструкция

См. файл `DOCKER_SETUP.md`

## 🔄 Миграция с обычной версии

Если у вас уже есть обычная версия (`app.py`), просто используйте `app_docker.py`:

```bash
# Обычная версия (без сохранения)
streamlit run app.py

# Docker версия (с MinIO и PostgreSQL)
docker-compose up
```

Обе версии могут работать параллельно!

