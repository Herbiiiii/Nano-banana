# 📦 Где хранятся данные и изображения

## 🔍 Обзор хранения данных

### ✅ Что НЕ сохраняется на диск:

1. **Референсные изображения** (загруженные файлы)
   - Хранятся **только в памяти** браузера (Streamlit session)
   - **Не сохраняются** на сервере или диске
   - Удаляются при закрытии браузера/сессии

2. **Сгенерированные изображения**
   - Хранятся на **серверах Replicate** (не на вашем компьютере)
   - Возвращаются как **URL-ссылки**
   - Вы можете скачать их через кнопку "Скачать"

3. **История генераций**
   - Хранится **только в памяти** браузера (`st.session_state`)
   - Сохраняется только на время сессии
   - **Удаляется** при закрытии браузера

4. **API ключ**
   - Если введен в интерфейсе: хранится в **памяти сессии**
   - Если через переменную окружения: хранится в **системных переменных**
   - **НЕ сохраняется** в файлах

## 📍 Детальное описание

### 1. Референсные изображения

**Локальный запуск:**
```
Загруженный файл → Память браузера → Отправка в Replicate API → Удаление из памяти
```

**На Hugging Face Spaces:**
```
Загруженный файл → Временная память Space → Отправка в Replicate API → Удаление
```

**Важно:** 
- Файлы **НЕ сохраняются** на диск
- Они передаются напрямую в Replicate API
- После генерации файл удаляется из памяти

### 2. Сгенерированные изображения

**Где хранятся:**
- На **серверах Replicate** (облачное хранилище)
- Возвращаются как **URL-ссылки** (например: `https://replicate.delivery/...`)
- Ссылки действительны **ограниченное время** (обычно несколько дней)

**Что происходит:**
```
Replicate генерирует → Сохраняет на своих серверах → Возвращает URL → Вы скачиваете
```

**Локальное хранение:**
- Изображения **НЕ сохраняются автоматически**
- Вы должны **вручную скачать** через кнопку "Скачать"
- Скачанные файлы сохраняются в папку "Загрузки" вашего браузера

### 3. История генераций

**Где хранится:**
```python
st.session_state.generated_images = [
    {
        "timestamp": "2025-12-22 17:40:47",
        "prompt": "ваш промпт",
        "result": "https://replicate.delivery/...",  # URL изображения
        "params": {...}
    }
]
```

**Характеристики:**
- ✅ Хранится **только в памяти браузера**
- ✅ Доступна только **в текущей сессии**
- ❌ **Удаляется** при закрытии браузера
- ❌ **Не сохраняется** на диск
- ❌ **Не синхронизируется** между устройствами

**Ограничения:**
- Сохраняется только **последние 5 генераций**
- Старые записи автоматически удаляются

### 4. API ключ

**Способ 1: Ввод в интерфейсе**
```
Ввод в UI → st.session_state → Память сессии → Удаляется при закрытии
```

**Способ 2: Переменная окружения**
```
Windows: $env:REPLICATE_API_TOKEN='ключ'
Linux/Mac: export REPLICATE_API_TOKEN='ключ'
→ Хранится в системных переменных (не в файлах проекта)
```

**Способ 3: Hugging Face Secrets**
```
Настройки Space → Secrets → REPLICATE_API_TOKEN
→ Хранится в зашифрованном виде на серверах HF
```

## 🔒 Безопасность и приватность

### ✅ Безопасно:

- Референсные изображения **не сохраняются** на сервере
- API ключ **не попадает** в код или файлы
- История **не сохраняется** между сессиями
- Данные **не передаются** третьим лицам (кроме Replicate API)

### ⚠️ Важно знать:

1. **Replicate видит ваши изображения**
   - Референсные изображения отправляются в Replicate API
   - Сгенерированные изображения хранятся на их серверах
   - Проверьте их [политику конфиденциальности](https://replicate.com/privacy)

2. **URL изображений временные**
   - Ссылки на сгенерированные изображения могут стать недействительными
   - **Скачивайте** важные результаты сразу

3. **На Hugging Face Spaces**
   - Все данные хранятся **в памяти** Space
   - При перезапуске Space данные теряются
   - Логи могут содержать URL изображений (но не сами изображения)

## 💾 Как сохранить данные локально

### Сохранение сгенерированных изображений:

1. **Через кнопку "Скачать"** в интерфейсе
   - Файлы сохраняются в папку "Загрузки"
   - Имя файла: `nano_banana_YYYYMMDD_HHMMSS.png`

2. **Программно** (если нужно):
```python
import requests
from pathlib import Path

# Скачать изображение
url = "https://replicate.delivery/..."
response = requests.get(url)
Path("saved_image.png").write_bytes(response.content)
```

### Сохранение истории (если нужно):

Можно добавить функцию экспорта:
```python
import json

# Экспорт истории
history = st.session_state.generated_images
with open("history.json", "w") as f:
    json.dump(history, f, indent=2)
```

## 📊 Схема хранения данных

```
┌─────────────────────────────────────────┐
│         ВАШ БРАУЗЕР (Локально)         │
├─────────────────────────────────────────┤
│ • Референсные изображения (память)     │
│ • История генераций (session_state)     │
│ • API ключ (если введен в UI)          │
│ • Временные данные сессии              │
└─────────────────────────────────────────┘
              ↓ (отправка)
┌─────────────────────────────────────────┐
│      REPLICATE API (Облако)             │
├─────────────────────────────────────────┤
│ • Референсные изображения (временно)  │
│ • Сгенерированные изображения (URL)     │
│ • Метаданные генераций                 │
└─────────────────────────────────────────┘
              ↓ (возврат URL)
┌─────────────────────────────────────────┐
│    ВАШ КОМПЬЮТЕР (Опционально)         │
├─────────────────────────────────────────┤
│ • Скачанные изображения (Загрузки/)    │
│ • API ключ (переменная окружения)      │
└─────────────────────────────────────────┘
```

## ❓ Часто задаваемые вопросы

**Q: Где физически хранятся мои загруженные изображения?**  
A: Только в памяти браузера во время сессии. Не сохраняются на диск.

**Q: Могу ли я восстановить историю после закрытия браузера?**  
A: Нет, история хранится только в памяти сессии.

**Q: Кто видит мои изображения?**  
A: Replicate API обрабатывает изображения согласно их политике конфиденциальности.

**Q: Как долго хранятся сгенерированные изображения на Replicate?**  
A: Обычно несколько дней, но точный срок не гарантирован. Скачивайте важные результаты.

**Q: Безопасно ли использовать на Hugging Face Spaces?**  
A: Да, данные хранятся в памяти и не сохраняются на диск. API ключ через Secrets безопасен.

